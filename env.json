{
  "PROXY_TO": {
    "description": "ArcGIS REST endpoint to pass converted requests to, including /arcgis/rest/services/",
    "required": true
  },
  "DEFAULT_PARAMS": {
    "description": "Querystring parameters to pass along by default, in a=b&c=d format",
    "required": false
  },
  "DEBUG": {
    "description": "Enable debug mode to log activity to console",
    "required": false
  }
}
