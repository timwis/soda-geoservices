{
  "PROXY_TO": {
    "description": "ArcGIS REST endpoint to pass converted requests to, ex: http://domain.com/arcgis/rest/services",
    "required": true
  },
  "DEFAULT_PARAMS": {
    "description": "Querystring parameters to pass along by default, ex: a=b&c=d",
    "required": false
  },
  "DEBUG": {
    "description": "Enable debug mode to log activity to console (true or false)",
    "required": false
  }
}
